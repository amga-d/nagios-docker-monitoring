name: ci

on:
  push:
    branches:
      - "build"

        
env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true


jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PAT }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: cloud
          endpoint: "a9mgad/nagios-test"

      - name: Build images from compose file
        env:
          DOCKER_USER: ${{ vars.DOCKER_USER }}
        run: docker compose -f docker-compose.yml build web dbserver
        
      - name: Push images to Docker Hub
        env:
          DOCKER_USER: ${{ vars.DOCKER_USER }}
        run: |
          docker compose -f docker-compose.yml push web dbserver
         
#      - name: Build and push
#        uses: docker/build-push-action@v6
#        with:
#          tags: "${{ vars.DOCKER_USER }}/docker-build-cloud-demo:latest"
#          # For pull requests, export results to the build cache.
#          # Otherwise, push to a registry.
#          outputs: ${{ github.event_name == 'pull_request' && 'type=cacheonly' || 'type=registry' }}
